<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="actionBarStyle">@style/OviedoTourGuideAppBarStyle</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- App bar style -->
    <style name="OviedoTourGuideAppBarStyle" parent="@style/Widget.AppCompat.Light.ActionBar.Solid.Inverse">
        <!-- Remove the shadow below the app bar -->
        <item name="elevation">0dp</item>
        <item name="background">@color/colorPrimary</item>
    </style>

    <!-- Style for a tab that displays a category name -->
    <style name="TabTitles" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/primary_text_color</item>
        <item name="tabSelectedTextColor">@color/primary_text_color</item>
        <item name="tabTextAppearance">@style/CategoryTabTextAppearance</item>
    </style>

    <!-- Text appearance style for a category tab -->
    <style name="CategoryTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">@color/secondary_text_color</item>
    </style>
</resources>
